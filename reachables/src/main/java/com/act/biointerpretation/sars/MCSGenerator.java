package com.act.biointerpretation.sars;

import java.util.Optional;

public class MCSGenerator implements SarGenerator {

  @Override
  public Optional<Sar> getSar(SeqGroup group) {
    return Optional.empty();
  }
}
